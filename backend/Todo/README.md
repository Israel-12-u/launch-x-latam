# Instalar SaSS compilador de CSS

npm install node-sass

## Si no funciona de esta manera
npm install -g node-sass

## agregar a pakage.json si no funciona la instalacion lo siguiente a nivel de dependencias
"node-sass": "^8.0.0"